# Spring
spring:
  grpc:
    server:
      port: 9090
      host: "*"
      reflection:
        enabled: false
  main:
    web-application-type: none
  application:
    name: "redis-bouncer"
  data:
    redis:
      client-type: lettuce
      host: "localhost"
      port: 6379
      lettuce:
        pool:
          enabled: true
          time-between-eviction-runs: 60s
          max-wait: -1ms
          min-idle: 0
          max-idle: -1
          max-active: -1
  threads:
    virtual:
      enabled: true